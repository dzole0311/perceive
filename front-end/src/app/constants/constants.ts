// The threshold that needs to be surpassed so that a high CPU
// load tracking would begin
export const CPU_HIGH_LOAD_THRESHOLD = 100;

// The duration after which we notify the user of a high CPU load
export const CPU_HIGH_LOAD_DURATION = 120;


// Threshold stepper button options
export const STEPS = 10;
export const CPU_LOAD_THRESHOLD_UPPER_LIMIT = 100;
export const CPU_LOAD_THRESHOLD_LOWER_LIMIT = 10;
export const CPU_DURATION_UPPER_LIMIT = 120;
export const CPU_DURATION_LOWER_LIMIT = 10;
