// The threshold that needs to be surpassed so that a high CPU load tracking would begin
export const CPU_HIGH_LOAD_THRESHOLD = 10;

// The duration after which we notify the user of a high CPU load
export const CPU_HIGH_LOAD_DURATION = 10

// The total time (10 minutes) for the window of historical CPU load information
export const TIME_WINDOW = 600;
