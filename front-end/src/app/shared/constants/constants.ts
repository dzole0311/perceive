// The threshold that needs to be exceeded in order for the high CPU load tracking to begin
export const CPU_HIGH_LOAD_THRESHOLD = 100;

// The duration that needs to be reached OR surpassed in order for the UI to switch to a HIGH_LOAD state
export const CPU_HIGH_LOAD_DURATION = 120;

// The total time (10 minutes) for the window of historical CPU load information
export const TIME_WINDOW = 600;
